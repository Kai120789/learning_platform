services:
  learning-platform-lessons:
    build:
      context: ..
      dockerfile: ./build/Dockerfile
    container_name: learning-platform-lessons
    restart: on-failure
    depends_on:
      - learning-platform-postgres
    ports:
      - "50053:50053"
    volumes:
      - ..:/app
